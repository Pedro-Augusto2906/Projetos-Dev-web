class ShoppingListManager:
    def __init__(self):
        self.shopping_list = [] 
        def add_item(self, item):
            # adciona um item á lista de compras
           self.shopping_list.append(item)
           print
            # remove um item da lista de compras
           def remove_item(self, item):
            # Exibi a lista de compras